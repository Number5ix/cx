#include <cx/ssdtree/ssdnode.sidl>

// Variant of SSDNode that exists only so that loaded JSON can remember the order of
// object keys when saving back.

class JSONNode extends SSDNode
{
    // order of keys in the children hashtable
    sarray:string keyorder;

    override createLike;

    factory create(SSDInfo *info);
}
