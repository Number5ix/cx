// Bare minimum for a task object

struct TaskQueue;
struct TaskResult;

[methodprefix btask] abstract class BasicTask {
    atomic:int32 state;
    [abstract] void run([in] TaskQueue *tq, [inout] TaskResult *result);
}
