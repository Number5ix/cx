// Bare minimum for a task object

struct TaskQueue;
struct TaskControl;

[methodprefix btask] abstract class BasicTask {
    atomic:int32 state;
    [abstract] bool run([in] TaskQueue *tq, [inout] TaskControl *tcon);
}
